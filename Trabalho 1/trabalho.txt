board( [ [a, b, c],
	     [d, e, f],
	     [g, h, i] ] ).

display_board([L1|Ls]):-
	write('|----------|'), nl,
	write(' | '), display_line(L1), nl,
	display_board(Ls).

display_board([]):-
	write('|----------|'), nl.

display_line([E1|Es]):-
	write(E1), write(' | '),
	display_line(Es).

display_line([]).